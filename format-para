#tag <p>: tag paragrap (phân đoạn)
#tag <br/>: break (dùng ngắt hàng)
#tag <b>: B"bold" (dùng bôi đậm)
#tag <strong>: giống thẻ <b> (dùng nhấn mạnh)
#tag <i>: Italic (in nghiên)
#tag <small>: (dùng định dạng chữ nhỏ)
#tag <mark>: đánh dấu highline (dùng bôi màu)
#tag <del>: (dùng gạch giữa văn bản)
#tag <sub>: (dùng nhảy xuống so với đoạn văn)
#tag <sup>: (dùng nhảy lên so với đoạn văn)